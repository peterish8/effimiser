/target
**/*.rs.bk
.ctx/
benchmarks/results/**/raw/
*.profraw

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
/loop prompt.txt

# Orchestration scratch: tickets and worker verdicts are committed as evidence,
# but raw worker transcripts are not.
.orchestration/**/*.log
.gstack/

# Local Claude Code session state, not project content.
.claude/
